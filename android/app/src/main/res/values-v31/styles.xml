<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Android 12+ (API 31) system splash. The OS always shows a splash on
         cold start; we can't remove it, but we hide the launcher-icon "logo"
         (transparent animated icon) and paint a plain black background that
         matches the JS SplashScreen artwork's near-black backdrop. Net effect:
         the user perceives a single splash (the JS artwork), not two. -->
    <style name="SplashTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_screen</item>
        <item name="android:windowSplashScreenBackground">#000000</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/splash_transparent</item>
    </style>

</resources>
